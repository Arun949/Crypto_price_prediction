import React from "react";
import ReactDOM from "react-dom/client";
import App from "/Users/arunkumaraluru/Desktop/crypto_price_prediction/frontend/src/App.tsx";

const root = ReactDOM.createRoot(document.getElementById("root") as HTMLElement);
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);